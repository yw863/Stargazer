/* 视图二骨架 — 待下一阶段实现 */
export default function PlanningForm({ event, onBack, onSubmit }) {
  return (
    <div style={{ paddingTop: '96px', color: 'var(--color-text-secondary)' }}>
      <p>视图二占位 · {event?.name}</p>
    </div>
  )
}
